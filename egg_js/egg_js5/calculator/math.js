// function palindrome() {
//     // Generar un número aleatorio entre -100 y 100
//     const randomNumber = Math.floor(Math.random() * 201) - 100; // Número aleatorio entre -100 y 100

//     // Imprimir el número aleatorio generado
//     console.log(`Número aleatorio: ${randomNumber}`);

//     // Aplicar los métodos de Math al número aleatorio
//     const absoluteValue = Math.abs(randomNumber);
//     console.log(`Math.abs(${randomNumber}) = ${absoluteValue}`);

//     const ceilingValue = Math.ceil(randomNumber);
//     console.log(`Math.ceil(${randomNumber}) = ${ceilingValue}`);

//     const floorValue = Math.floor(randomNumber);
//     console.log(`Math.floor(${randomNumber}) = ${floorValue}`);

//     const roundedValue = Math.round(randomNumber);
//     console.log(`Math.round(${randomNumber}) = ${roundedValue}`);

//     const maxValue = Math.max(randomNumber, 0); // Comparar con 0 para evitar valores negativos
//     console.log(`Math.max(${randomNumber}, 0) = ${maxValue}`);

//     const minValue = Math.min(randomNumber, 0); // Comparar con 0 para evitar valores positivos
//     console.log(`Math.min(${randomNumber}, 0) = ${minValue}`);

//     // Devolver el número aleatorio generado
//     return randomNumber;
// }

// // Ejecutar la función palindrome
// const result = palindrome();

// // Imprimir el resultado devuelto por la función
// console.log(`Resultado de palindrome(): ${result}`);
